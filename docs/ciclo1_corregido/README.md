# Corrected Cycle 1 deliverables

The Java foundation has already been corrected in the `slotMachine/` folder.

Before beginning Cycle 2, add these synchronized files here:

- `proyecto_ciclo1_corregido.asta`: class diagram and one sequence diagram for each public `SlotMachine` operation.
- `Retrospectiva_Ciclo1_corregida.docx`: retrospective updated with the real mini-cycle status, limitations and properly formatted references.

The corrected Astah design must use `Wheel` and English identifiers. Do not copy the original diagrams without synchronizing them with the current Java source.
